version: '3.8'

services:
  launcher:
    image: python:3
    volumes:
      - ./:/srv
    working_dir: /srv
    command: python3 -m http.server 8000
    ports:
      - "8000:8000"
    restart: unless-stopped
